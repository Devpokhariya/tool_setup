import streamlit as st

st.title("Move to Microsoft Teams")
st.header("Enter your Details -")
phone = st.text_input("Mobile Number")
url = st.text_input("User URl")
name = st.text_input("User Name")
# terminal_value = 0
# current_val = initial
# for year in range(int(yr)):
#    current_val += growth * current_val
#    terminal_value = current_val

# perform cashflow projections for the next 5 years
# st.write(f'Hello {name} moving your user from skype to teams. your number {phone} & url is {url}')